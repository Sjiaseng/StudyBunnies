import 'package:flutter/material.dart';

class Quiztestlist extends StatelessWidget {
  const Quiztestlist({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Student Quiz/Test Page"),
    );
  }
}
